<Window x:Class="RSTGameTranslation.TranslatedPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RSTGameTranslation"
        Title="Translated Preview"
        Height="500" Width="700"
        MinHeight="200" MinWidth="300"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e"
        Icon="/media/AppIcon.ico"
        Closing="Window_Closing">
    <Grid>
        <Viewbox Stretch="Uniform" StretchDirection="Both">
            <Grid x:Name="previewContainer">
                <Image x:Name="previewImage"
                       Stretch="None"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <Canvas x:Name="previewOverlayCanvas"
                        Panel.ZIndex="1"
                        IsHitTestVisible="False"/>
            </Grid>
        </Viewbox>

        <Border VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Background="#AA000000"
                Padding="8,4"
                CornerRadius="4,0,0,0"
                Panel.ZIndex="10">
            <TextBlock x:Name="previewStatusText"
                       Foreground="#888"
                       FontSize="11"
                       Text="Waiting for translation..."/>
        </Border>
    </Grid>
</Window>
